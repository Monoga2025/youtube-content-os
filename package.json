{
  "name": "youtube-content-os",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check lib/local-core.js && node --check lib/store.js && node --check bin/youtube-content-os.js && node --check apps-script/Core.js && node --check apps-script/Code.js && node --test tests/*.test.js",
    "local": "node bin/youtube-content-os.js"
  },
  "engines": {
    "node": ">=24"
  }
}

