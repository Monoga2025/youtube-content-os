'use strict';
const fs=require('node:fs'),path=require('node:path'),crypto=require('node:crypto');const {DatabaseSync}=require('node:sqlite');const {profileDefinition,redact}=require('./local-core');
const baseRoot=()=>process.env.YOUTUBE_CONTENT_OS_HOME||path.join(process.env.LOCALAPPDATA||path.join(process.env.HOME||'.','AppData','Local'),'Monoga','YouTubeContentOS');
function root(profile){const p=profileDefinition(profile);return path.join(baseRoot(),'profiles',p.id);}
function open(profile){if(process.argv.includes('--test')&&!process.env.YOUTUBE_CONTENT_OS_HOME)throw new Error('test store requires YOUTUBE_CONTENT_OS_HOME');const definition=profileDefinition(profile);const dir=root(definition.id);fs.mkdirSync(dir,{recursive:true});const db=new DatabaseSync(path.join(dir,'youtube-content-os.sqlite'));db.exec(`PRAGMA foreign_keys=ON; PRAGMA busy_timeout=5000; CREATE TABLE IF NOT EXISTS config(k TEXT PRIMARY KEY,v TEXT NOT NULL); CREATE TABLE IF NOT EXISTS sources(id TEXT PRIMARY KEY, channel_id TEXT UNIQUE NOT NULL, enabled INTEGER NOT NULL DEFAULT 0, owner TEXT NOT NULL DEFAULT '', created_at TEXT NOT NULL); CREATE TABLE IF NOT EXISTS signals(id TEXT PRIMARY KEY,source_id TEXT NOT NULL REFERENCES sources(id) ON DELETE CASCADE,video_id TEXT UNIQUE NOT NULL,title TEXT,published_at TEXT,views INTEGER NULL,fetched_at TEXT NOT NULL,human_note TEXT NOT NULL DEFAULT ''); CREATE TABLE IF NOT EXISTS signal_notes(video_id TEXT PRIMARY KEY,human_note TEXT NOT NULL,created_at TEXT NOT NULL); CREATE TABLE IF NOT EXISTS evidence(id TEXT PRIMARY KEY,source_id TEXT REFERENCES sources(id) ON DELETE SET NULL,body TEXT NOT NULL,created_at TEXT NOT NULL,source_reference TEXT NULL,verified_at TEXT NULL); CREATE TABLE IF NOT EXISTS feedback(id TEXT PRIMARY KEY,piece_id TEXT NOT NULL,date TEXT,native_retention TEXT,icp_signal TEXT,production_minutes INTEGER,notes TEXT,created_at TEXT NOT NULL); CREATE TABLE IF NOT EXISTS missions(id TEXT PRIMARY KEY,title TEXT NOT NULL,status TEXT NOT NULL DEFAULT 'DRAFT' CHECK(status IN ('DRAFT','READY','ARCHIVED')),source_id TEXT,created_at TEXT NOT NULL); CREATE TABLE IF NOT EXISTS run_log(id TEXT PRIMARY KEY,kind TEXT NOT NULL,status TEXT NOT NULL,detail TEXT,created_at TEXT NOT NULL);`);for(const statement of ["ALTER TABLE sources ADD COLUMN owner TEXT NOT NULL DEFAULT ''","ALTER TABLE evidence ADD COLUMN source_reference TEXT NULL","ALTER TABLE evidence ADD COLUMN verified_at TEXT NULL"])try{db.exec(statement)}catch{};return db;}
const id=()=>crypto.randomUUID(),now=()=>new Date().toISOString();function cfg(db,k,v){if(arguments.length===3)db.prepare('INSERT OR REPLACE INTO config VALUES (?,?)').run(k,String(v));return db.prepare('SELECT v FROM config WHERE k=?').get(k)?.v;}function log(db,kind,status,detail){db.prepare('INSERT INTO run_log VALUES (?,?,?,?,?)').run(id(),kind,status,redact(detail||'').slice(0,240),now());}function transaction(db,fn){db.exec('BEGIN IMMEDIATE');try{const value=fn();db.exec('COMMIT');return value;}catch(e){db.exec('ROLLBACK');throw e;}}
module.exports={baseRoot,root,open,id,now,cfg,log,transaction};